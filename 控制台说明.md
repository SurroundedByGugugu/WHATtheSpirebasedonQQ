# 控制台说明

控制台用于测试或临时调整当前 Run。它不是正常游玩指令，使用前需要已经用 `/card new` 开始一局。

## 怎么输入

把普通 `/card` 指令开头换成控制台开头。

控制台开头可以使用：

- `/ctrl`
- `.ctrl`
- `。ctrl`

例如：

```text
/ctrl addgold 99
.ctrl addcard 打击+ 手牌
。ctrl addhp 99
```

## 通用规则

- 数量不写时默认是 1。
- 卡牌、遗物、状态可以直接写中文名。
- `addpotion` 会受当前药水栏位上限限制；先扩栏位再添加即可塞更多。
- 只影响当前会话里的当前 Run。
- 测试房间不会出现在正常路线中，只能通过测试入口进入。
- 修改手牌、敌人状态、Zone 时，需要当前正在战斗中。
- `removestate` 不写数量时，会直接清空该状态。
- 药水栏位被调低到少于已有药水数量时，会从末尾丢弃超出的药水。

## 进入测试房间

```text
/ctrl testroom 类型
```

类型：

- `battle`：进入本地硬编码测试战斗房间
- `event`：进入本地硬编码测试事件房间

例子：

```text
/ctrl testroom battle
/ctrl testroom event
```

测试战斗房间会基于本地代码组装 character、relic、potion、deck、status、zone/field、encounter.enemy等。进入后仍可继续使用其他 `/ctrl` 指令调整当前房间。

## 添加卡牌

```text
/ctrl addcard 卡牌名 牌堆 [数量]
```

例子：

```text
/ctrl addcard 打击+ 手牌
/ctrl addcard 粘液 弃牌堆
/ctrl addcard 伤口 抽牌堆 3
```

可用牌堆：

- 手牌
- 抽牌堆
- 弃牌堆
- 消耗牌堆
- 牌库

## 移除卡牌

```text
/ctrl removecard 卡牌名 牌堆 [数量]
```

例子：

```text
/ctrl removecard 粘液 弃牌堆
/ctrl removecard 打击 手牌 2
```

## 添加遗物

```text
/ctrl addrelic 遗物名 [数量]
```

例子：

```text
/ctrl addrelic 墨水瓶
/ctrl addrelic 开心小花 2
```

添加遗物会触发拾取效果。

## 移除遗物

```text
/ctrl removerelic 遗物名 [数量]
```

例子：

```text
/ctrl removerelic 开心小花
```

## 添加药水

```text
/ctrl addpotion 药水名 [数量]
```

例子：

```text
/ctrl addpotion 攻击药水
/ctrl addpotion 果汁 2
```

药水栏已满时不会添加。可以先用 `addpotionslots` 或 `setpotionslots` 调整栏位。

## 移除药水

```text
/ctrl removepotion 药水名 [数量]
/ctrl removepotion 药水编号
```

例子：

```text
/ctrl removepotion 攻击药水
/ctrl removepotion 0
```

药水编号可用 `/card potions` 查看。

## 调整药水栏位

```text
/ctrl setpotionslots 数量
/ctrl addpotionslots 数量
```

例子：

```text
/ctrl setpotionslots 4
/ctrl addpotionslots 1
/ctrl addpotionslots -1
```

`setpotionslots` 会把药水栏位设置为指定数量，`addpotionslots` 会在当前栏位基础上增减。

## 添加状态

```text
/ctrl addstate 状态名 [数量] [目标]
```

例子：

```text
/ctrl addstate 力量 1 self
/ctrl addstate 易伤 1 enemy[0]
/ctrl addstate 易伤 enemy[0]
```

目标：

- `self`：玩家自己
- `enemy[0]`：第 0 个敌人
- `enemy[1]`：第 1 个敌人

## 移除状态

```text
/ctrl removestate 状态名 [数量] [目标]
```

例子：

```text
/ctrl removestate 易伤 enemy[0]
/ctrl removestate 力量 2 self
```

不写数量时，会清空该状态。

## 清空当前房间怪物

```text
/ctrl clearenemies
```

例子：

```text
/ctrl clearenemies
```

会清空当前战斗中的怪物，并按战斗胜利处理。

## 设置 Zone

```text
/ctrl addzone Zone名
```

例子：

```text
/ctrl addzone fire
/ctrl addzone 火
/ctrl addzone extreme_crystal
/ctrl addzone 极晶
```

`extreme_` 或 `极` 开头表示极 Zone。

## 增加生命

```text
/ctrl addhp 数量
```

例子：

```text
/ctrl addhp 99
```

超过最大生命时，只会加到最大生命。

## 增加最大生命

```text
/ctrl addmaxhp 数量
```

例子：

```text
/ctrl addmaxhp 99
```

最大生命增加时，当前生命也会一起增加。

## 增加费用

```text
/ctrl addcost 数量
```

例子：

```text
/ctrl addcost 3
```

只修改当前战斗中的当前费用，不修改每回合费用上限。可以填负数扣回去，最低为 0。

## 增加金币

```text
/ctrl addgold 数量
```

例子：

```text
/ctrl addgold 99
```

## 常用复制区

```text
/ctrl addcard 打击+ 手牌
/ctrl removecard 粘液 弃牌堆
/ctrl addrelic 墨水瓶
/ctrl removerelic 开心小花
/ctrl addpotion 攻击药水
/ctrl removepotion 0
/ctrl setpotionslots 4
/ctrl addpotionslots 1
/ctrl addstate 易伤 1 enemy[0]
/ctrl addstate 力量 1 self
/ctrl removestate 易伤 enemy[0]
/ctrl addzone extreme_crystal
/ctrl addhp 99
/ctrl addmaxhp 99
/ctrl addcost 3
/ctrl addgold 99
/ctrl testroom battle
/ctrl testroom event
/ctrl clearenemies
```
