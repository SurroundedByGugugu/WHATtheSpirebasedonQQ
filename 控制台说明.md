# 控制台说明

控制台用于测试或临时调整当前 Run。它不是正常游玩指令，使用前需要已经用 `/card new` 开始一局。

## 怎么输入

把普通 `/card` 指令开头换成 `/ctrl`。

也可以使用：

- `.ctrl`
- `。ctrl`
- `ctrl`

例如：

```text
/ctrl addgold 99
ctrl addcard 打击+ 手牌
```

## 通用规则

- 数量不写时默认是 1。
- 卡牌、遗物、状态可以直接写中文名。
- 只影响当前会话里的当前 Run。
- 修改手牌、敌人状态、Zone 时，需要当前正在战斗中。
- `removestate` 不写数量时，会直接清空该状态。

## 添加卡牌

```text
/ctrl addcard 卡牌名 牌堆 [数量]
```

例子：

```text
/ctrl addcard 打击+ 手牌
/ctrl addcard 粘液 弃牌堆
/ctrl addcard 伤口 抽牌堆 3
```

可用牌堆：

- 手牌
- 抽牌堆
- 弃牌堆
- 消耗牌堆
- 牌库

## 移除卡牌

```text
/ctrl removecard 卡牌名 牌堆 [数量]
```

例子：

```text
/ctrl removecard 粘液 弃牌堆
/ctrl removecard 打击 手牌 2
```

## 添加遗物

```text
/ctrl addrelic 遗物名 [数量]
```

例子：

```text
/ctrl addrelic 墨水瓶
/ctrl addrelic 开心小花 2
```

添加遗物会触发拾取效果。

## 移除遗物

```text
/ctrl removerelic 遗物名 [数量]
```

例子：

```text
/ctrl removerelic 开心小花
```

## 添加状态

```text
/ctrl addstate 状态名 [数量] [目标]
```

例子：

```text
/ctrl addstate 力量 1 self
/ctrl addstate 易伤 1 enemy[0]
/ctrl addstate 易伤 enemy[0]
```

目标：

- `self`：玩家自己
- `enemy[0]`：第 0 个敌人
- `enemy[1]`：第 1 个敌人

## 移除状态

```text
/ctrl removestate 状态名 [数量] [目标]
```

例子：

```text
/ctrl removestate 易伤 enemy[0]
/ctrl removestate 力量 2 self
```

不写数量时，会清空该状态。

## 设置 Zone

```text
/ctrl addzone Zone名
```

例子：

```text
/ctrl addzone fire
/ctrl addzone 火
/ctrl addzone extreme_crystal
/ctrl addzone 极晶
```

`extreme_` 或 `极` 开头表示极 Zone。

## 增加生命

```text
/ctrl addhp 数量
```

例子：

```text
/ctrl addhp 99
```

超过最大生命时，只会加到最大生命。

## 增加最大生命

```text
/ctrl addmaxhp 数量
```

例子：

```text
/ctrl addmaxhp 99
```

最大生命增加时，当前生命也会一起增加。

## 增加金币

```text
/ctrl addgold 数量
```

例子：

```text
/ctrl addgold 99
```

## 常用复制区

```text
/ctrl addcard 打击+ 手牌
/ctrl removecard 粘液 弃牌堆
/ctrl addrelic 墨水瓶
/ctrl removerelic 开心小花
/ctrl addstate 易伤 1 enemy[0]
/ctrl addstate 力量 1 self
/ctrl removestate 易伤 enemy[0]
/ctrl addzone extreme_crystal
/ctrl addhp 99
/ctrl addmaxhp 99
/ctrl addgold 99
```
