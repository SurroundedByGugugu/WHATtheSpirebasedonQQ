# 群内多人游戏说明

当前多人模式是一个独立的多人测试房，不是完整多人爬塔。它复用现有角色、卡牌、敌人、药水、遗物内容，但底层房间、战斗、奖励结算是新建的简化框架。

命令中的 `/card` 也可以按原项目习惯替换成 `.card` 或 `。card`。

## 开房与加入

```text
/card multi new [角色编号]
/card multi join [角色编号]
/card multi start
/card multi view
/card multi close
```

- `new`：开一桌多人测试房，并让开房者加入。开房者是房主。
- `join`：加入当前群的多人测试房；开战前重复使用可换角色。
- `start`：开始一场测试战，只有房主可以使用。
- `view`：查看当前房间、战斗或奖励状态。
- `close`：关闭当前多人测试房，只有房主可以使用。

当前同一个群会话同一时间只维护一间多人测试房，人数上限为 6 人。开战后不能再加入或换角色。

## 角色编号

```text
0 测试角色
1 铁甲战士
2 静默猎手
3 昼·里辛塔法
4 Yoirine
5 Suzuri
```

不填角色编号时，默认使用 `0 测试角色`。

## 战斗规则

```text
/card multi play 手牌编号 [敌人编号]
/card multi end
```

- 开战时，敌人从一层前半的 starting encounter 池中随机。
- 敌人的最大生命和当前生命会按参战人数倍率放大。
- 每名玩家使用自己的角色、HP、费用、手牌、抽牌堆、弃牌堆、消耗堆、遗物和药水栏。
- 当前行动者才能出牌或结束回合。
- `play` 会打出指定手牌；需要敌方目标的牌可追加敌人编号，不填时默认选择第一个存活敌人。
- `end` 会结束当前玩家回合，轮到下一名未结束回合的存活玩家。
- 所有存活玩家都结束回合后，敌人行动。
- 敌人的同一个行动会对所有存活玩家执行一遍。
- 所有敌人死亡后进入多人奖励阶段；所有玩家倒下则房间失败。

当前多人测试房只实现了常见卡牌效果，例如伤害、格挡、抽牌、加能量、施加状态。复杂卡牌、复杂敌人行动、药水使用、完整遗物触发还在后续扩展中；未实现效果会显示“暂未处理”。

## 奖励规则

```text
/card multi reward
/card multi relic 编号
/card multi relic skip
```

战斗胜利后会进入多人奖励阶段：

- 每名玩家获得相同数量金币。
- 每名玩家独立随机一瓶药水；如果药水栏未满，会加入自己的药水栏。
- 遗物会随机出一组，数量等于参战人数。
- 有所属角色的遗物只会在该角色有玩家参战时进入池子。
- 同一组奖励中，某角色所属遗物数量不会超过使用该角色的玩家数量。

每名存活玩家需要选择一个遗物编号，或使用 `skip` 跳过。系统会等待所有存活玩家完成选择后统一结算。

如果多人选择同一个遗物，会自动进行 d100 掷骰，点数更小者获得该遗物。当前版本没有二轮补选，争夺失败者本次不会重新选择其他遗物。

## 推荐流程

```text
/card multi new 1
/card multi join 2
/card multi join 5
/card multi start
/card multi play 0 0
/card multi end
/card multi reward
/card multi relic 0
```

先让所有参与者加入并确认角色，再由任意人使用 `start` 开始测试战。战斗中多用 `view` 查看当前行动者、手牌和敌人状态。
